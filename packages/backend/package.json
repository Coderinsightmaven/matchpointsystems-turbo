{
  "name": "@workspace/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "predev": "convex dev --until-success && convex dev --once --run-sh \"node ../../apps/web/setup.mjs --once\" && convex dashboard"
  },
  "dependencies": {
    "@convex-dev/auth": "^0.0.90",
    "convex": "^1.31.4"
  }
}
